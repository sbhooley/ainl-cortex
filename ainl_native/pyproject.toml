[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ainl_native"
version = "0.1.0"
description = "Native Rust bindings for ainl-graph-memory plugin"
requires-python = ">=3.10"

[tool.maturin]
module-name = "ainl_native"
features = ["pyo3/extension-module"]
